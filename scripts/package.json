{
	"name": "postgres-backup",
	"version": "1.0.0",
	"description": "Script to backup postgres database",
	"scripts": {
		"backup": "tsc && node dist/backup.js",
		"restore": "tsc && node dist/restore.js",
		"format": "prettier --check --ignore-path .gitignore .",
		"pretty": "prettier --write --ignore-path .gitignore ."
	},
	"keywords": [],
	"author": "",
	"license": "MIT",
	"dependencies": {
		"node-cron": "^3.0.3",
		"ts-postgres": "^1.8.0"
	},
	"devDependencies": {
		"@types/node": "^20.10.0",
		"@types/node-cron": "^3.0.11",
		"eslint": "^8.54.0",
		"prettier": "^2.8.8",
		"typescript": "^5.3.2"
	}
}
